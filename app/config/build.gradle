buildscript {
    apply from: "$rootProject.projectDir/gradle/buildscript.gradle", to: buildscript
}

description = "Example - App Config"
defaultTasks = ['appConfigDeploy']

apply plugin: 'com.cognifide.aem'

build.dependsOn aemCompose
task appConfigDeploy(dependsOn: [build, aemDeploy])